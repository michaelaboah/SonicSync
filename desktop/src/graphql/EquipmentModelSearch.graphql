query EquipmentModelSearch($fullSearch: String!) {
  fullTextSearch(fullSearch: $fullSearch) {
    # id
    created_at
    updated_at
    category
    manufacturer
    model
    public_notes
    cost
    powerDraw
    weight
    depth
    rackUnit
    frequencyRange
  }
}
